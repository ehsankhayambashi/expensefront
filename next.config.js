// next.config.js

const nextConfig = {
    images: {
      domains: [
        'localhost', // Your local development server
        'jibsanj.ir', // Production domain
      ],
    },
  };
  
  module.exports = nextConfig;
  